You must have both [nodeJS](https://nodejs.org/en/download/package-manager/) and [yarn](https://yarnpkg.com/lang/en/docs/install) installed.

To install dependencies:

```
    $ yarn install
```

To run tests:

```
    $ yarn test
```